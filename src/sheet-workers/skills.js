var stats = data.stats;
var skills = data.skills;

//- var calcSkillEvents = [
//-   'change:Damage_Inidcator',
//-   'sheet:opened'
//- ];
//- calcSkillEvents = calcSkillEvents.join(' ').toLowerCase();

//- on(calcSkillEvents, function(eventInfo) {

//- })
